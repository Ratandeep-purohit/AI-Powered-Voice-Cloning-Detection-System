# Backend Dependencies – Phase 00 Foundation + Phase 02 Auth

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Data validation & settings
pydantic==2.10.3
pydantic-settings==2.6.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Authentication – Phase 02
argon2-cffi==23.1.0         # Argon2 password hashing (SECURITY.md required)
PyJWT==2.8.0                 # JWT creation and validation
cryptography==42.0.8         # Required for PyJWT RS/HS algorithms
python-multipart==0.0.9      # Multipart form support for login

# Rate limiting – Phase 02
slowapi==0.1.9               # FastAPI-compatible rate limiter

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1

# Utilities
python-dotenv==1.0.1
